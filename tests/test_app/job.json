{
  "name": "{{ app.name }}-job-test",
  "appId": "{{ app.name }}--{{ app.version}}",
  "nodeCount": 1,
  "processorsPerNode": 2,
  "memoryPerNode": "1",
  "maxRunTime": "00:01:00",
  "inputs": {"target_file":"./tests/test_input_file"},
  "parameters": {"command": "ls", "opts": "-alth"}
}
